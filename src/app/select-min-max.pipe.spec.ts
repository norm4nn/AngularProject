import { SelectMinMaxPipe } from './select-min-max.pipe';

describe('SelectMinMaxPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectMinMaxPipe();
    expect(pipe).toBeTruthy();
  });
});
